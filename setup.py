from setuptools import setup

setup(name='tracktor',
      packages=['tracktor'],
      package_dir={'':'src'},
      version='0.0.1',
      install_requires=[],)

setup(name='ml',
      packages=['ml'],
      package_dir={'':'src'},
      version='0.0.1',
      install_requires=[],)
